.hero-image
  .hero-image__background(:style='"backgroundImage: url(" + props.background + ");"')
    .hero-image__title(v-html='props.title')
  .hero-home__waypoint-scroll(v-waypoint='{ active: true, callback: scrolled}')
